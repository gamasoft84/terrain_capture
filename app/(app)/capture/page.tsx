import {
  Card,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";

export default function CapturePlaceholderPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Capturar</CardTitle>
        <CardDescription>
          Flujo de captura de vértices (GPS + foto): tareas 1.6–1.9.
        </CardDescription>
      </CardHeader>
    </Card>
  );
}
